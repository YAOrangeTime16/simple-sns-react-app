{
  "rules": {
    "users" : {
      	".read": "auth!=null",
        "$uid": {
          ".write": "$uid === auth.uid"
        }
    },
    "posts" : {
      ".read" : "true",
    	".write": "auth!=null"
    }
  }
}